# ClaimCase Workshop

<!-- DRAFT STUB — structure approved; content pass pending. Bullets below are the planned content of this page. -->

Planned content:

- Welcome + what this workshop is: build an enterprise-grade property-claims solution end-to-end with coding agents (level ~300, technical audience)
- Training-environment callout: log in at `{{ training_url }}`, tenant `{{ training_tenant }}` (macros)
- What you'll build in one glance: a Maestro case + seven agents + an Action App + the claim record (+ a dashboard, later)
- The path: Prepare → Plan → Build → Verify — one link per section
- Key-concepts glossary: coding agent, `uip` CLI, skills, seed, block, gate
