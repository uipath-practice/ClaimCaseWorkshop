# Architecture Overview

<!-- DRAFT STUB — structure approved; content pass pending. Bullets below are the planned content of this page. -->

Planned content:

- The full solution diagram: step by step, which build block produces which artifact and which downstream step consumes it
    - document payloads → the claim record (entity) → the agents' findings → the case plan → human tasks → the review screens
- How components talk and exchange data: payloads, the entity, case variables, task schemas
- Cross-references into the SDD you just generated — the produced document is aligned with this architecture
- Objective: before Build starts, you know what each block is going to add and where its output goes
