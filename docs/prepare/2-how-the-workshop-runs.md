# How the Workshop Runs

<!-- DRAFT STUB — structure approved; content pass pending. Bullets below are the planned content of this page. -->

Planned content:

- The stage diagram: Plan (design + tasks) → Build (six blocks) → Verify → Ship — the seed-block ↔ course-section map, so the seed's sequence table and this site line up
- How a block works: prompt → your agent builds → gate → *go and look at it*
- Working habits:
    - clear context at block boundaries (context-size table: what fits in one session)
    - `PROGRESS.md` is the cross-block memory — append as you go
    - upload the solution to Studio Web at the end of every block
- Gates are commands, not opinions: read the checker's rules even when it passes; never edit a document to satisfy a rule you believe is wrong — log it instead
- Logging findings is part of the deliverable (`log-finding.py`)
- Rough timing for the day, per block
