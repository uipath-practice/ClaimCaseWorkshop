# Get the Seed

<!-- DRAFT STUB — structure approved; content pass pending. Bullets below are the planned content of this page. -->

Planned content:

- Get the seed: clone (the taught path) vs zip download
- Map of the seed: `README.md`, `PDD.md`, `CONFIG.md`, `contracts/`, `method/`, `known-issues/`, and the block folders (each = `prompt.md` + `cookbook.md`, sometimes a gate script)
- What is provided vs what you build: bind, don't build — seven agents + one app, nothing else; the claim generator is provided (its dials are explained in Verify, where aiming matters)
- Artifacts you will produce: `sdd.md`, `tasks.md`, the solution (case + agents), the Action App, a filled `PROGRESS.md`, and — at the end — a runbook
- Pre-flight checks before block 1
